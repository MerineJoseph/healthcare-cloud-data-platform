# Logging and Monitoring Layer

## Purpose

The logging layer records each major ETL pipeline event, including extraction, transformation, data quality checks, loading, success, and failure.

## Local Logging

Logs are written to:

```text
logs/healthcare_pipeline.log
